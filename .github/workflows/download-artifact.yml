name: download-artifact
on: 
  workflow_call:
jobs:
  download-artifact:
    name: Download artifact 
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v3
      - name: Write smth
        run: echo "It works"