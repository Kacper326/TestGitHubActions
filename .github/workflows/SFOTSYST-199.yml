name: md validation splitted with github ifs
run-name: ${{ github.actor }} is testing out github if with tj-actions/verify-changed-files
on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/different-dir-different-action.yml
      - force-app/base/*
      - force-app/framework/*
jobs:
  create-base-pkg:
    name: Try Creating the Packages
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout the Codebase
        uses: actions/checkout@v3
        - name: Check if directory has changes
        id: directory_changes_checker
        uses: georgeneto/directory-changes-checker@v1
        with:
          repo-token: ${{secrets.GITHUB_TOKEN}}
          directory: "{base/}"

      - name: Any other step
        if: steps.directory_changes_checker.output.hasChanges == 'true'
        run: echo "Działa :o"
    #   - name: Echo all changes
    #     run: echo "Changed files ${{ steps.verify-changed-files.outputs.changed_files }}"
    #   - name: Echo base if changes in base
    #     if: contains(steps.verify-changed-files.outputs.changed_files, 'base')
    #     run: echo "Base"
    #   - name: Echo base if changes in framwork
    #     if: contains(steps.verify-changed-files.outputs.changed_files, 'framework')
    #     run: echo "Framework"
          
      #   - name: Verify if data model changed
      #     uses: tj-actions/verify-changed-files@v16
      #     id: data-model-change
      #     with:
      #         files: |
            
      #   - name: Echo Framework
      #     if: contains(env.CHANGED_FILES, 'force-app/framework/') != true 
      #     run: echo "Framework is in env.CHANGED_FILES "
      #   - name: Echo Base
      #     if: >- 
      #       (contains(env.CHANGED_FILES, 'force-app/base/') == true 
      #       &&  ${{ success() }})
      #       || (contains(env.CHANGED_FILES, 'force-app/base/') == true 
      #       &&  ${{ success() }})
      #     run: echo "Base is in env.CHANGED_FILES"
  